// All enums used across the schema

enum AccountStatus {
  active
  disabled
  suspended
  pending
}

enum VoteType {
  up
  down
}

enum RosterStatus {
  pending
  accepted
  declined
  blocked
}

enum MessageType {
  USER
  POST
  SYSTEM
}

enum NotificationType {
  FOLLOW
  VOTE
  COMMENT
  QUOTE
  MESSAGE
  MENTION
  SYSTEM
  UPVOTED
  DOWNVOTED
}

enum ActivityEventType {
  POSTED
  VOTED
  COMMENTED
  QUOTED
  LIKED
  UPVOTED
  DOWNVOTED
}

enum GroupPrivacy {
  public
  private
  restricted
}

enum InviteStatus {
  pending
  accepted
  declined
  expired
}

enum ReportStatus {
  pending
  reviewed
  resolved
  dismissed
}

enum ReportSeverity {
  low
  medium
  high
  critical
}

enum ReportReason {
  spam
  harassment
  inappropriate_content
  fake_account
  other
}
